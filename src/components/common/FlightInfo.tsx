import * as React from 'react';

import { NewFlights } from 'types/types'


export const FlightInfo = ({ details, ...rest }: any) => {
    console.log('props=', details)
    return (
        <div className="flight-card">

        </div>
    )
}
