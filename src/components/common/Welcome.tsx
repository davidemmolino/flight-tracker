import * as React from 'react';
import MainHeader from 'components/layout/MainHeader';

export const Welcome = () => {
    return (
        <MainHeader title='Welcome!'
            subtitle='Start by adding a city! props'
            onClickAction={() => { }} />
    )
}
