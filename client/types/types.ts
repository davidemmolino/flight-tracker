export interface newFlights {
    airline : string,
    location : string,
    arrival : string,
    departure : string,
}

export type FlightInfo = {
    location: string
    arrival: string
    departure: string
    airline: string
}